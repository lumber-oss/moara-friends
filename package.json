{
  "name": "moara-friends",
  "version": "1.0.0",
  "private": true,
  "description": "MOARA 友链数据源 —— 单文件一条 + GitHub Action 自动校验/合并/构建，jsDelivr 分发",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
